import type { FC } from 'react';

type Props = {
    item?: DetailVideoItem;
}

const Video: FC<Props> = ({ item }) => {



    return (
        <div>
           Video
        </div>
    );
};

export default Video;